package com.knownassurajit.app.game.impstr

import androidx.activity.ComponentActivity

/** Exported debug host so Compose UI tests attach to a real resumed window. */
class E2EHostActivity : ComponentActivity()
